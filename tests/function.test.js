const { test } = require("@jest/globals");
const { ensureActualIsNumber } = require("jest-matcher-utils");

// test("something descriptive"), () => {
//   //arrange 
//   actual =
//   expected =
test('it works', () => {
  expect(4).toBeLessThan(5)
})

  //act
  
  //assert
  // done()
